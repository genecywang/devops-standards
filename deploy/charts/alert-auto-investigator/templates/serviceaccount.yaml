{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "alert-auto-investigator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "alert-auto-investigator.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
